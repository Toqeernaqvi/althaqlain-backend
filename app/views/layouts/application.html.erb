<!DOCTYPE html>
<html>
  <head>
    <title>HotelManagement</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <script type="module" src="https://unpkg.com/ionicons@7.1.0/dist/ionicons/ionicons.esm.js"></script>
    <script nomodule src="https://unpkg.com/ionicons@7.1.0/dist/ionicons/ionicons.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag 'application', 'data-turbo-track': 'reload', defer: true %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="sidebar bg-dark text-white" aria-label="Sidebar navigation">
      <div class="container-fluid">
        <%= link_to "Althaqlain", root_path, class: "navbar-brand text-white logo" %>
        <hr>
        <ul class="navbar-nav flex-column">
          <% if admin_signed_in? %>
            <li class="nav-items btn btn-dark">
              <ion-icon name="log-in-outline" class="icons"></ion-icon>
              <span class="navbar-text">Logged in as: <%= current_admin.email %></span>
            </li>
            <li class="nav-items btn btn-dark">
              <ion-icon name="bed-outline" class="icon"></ion-icon>
              <%= link_to "Hotels", hotels_path %>
            </li>
            <li class="login btn btn-danger">
            <ion-icon name="power-outline" class="icon"></ion-icon>
              <%= link_to "Log out", destroy_admin_session_path, method: :delete %>
            </li>
          <% else %>
            <li class="nav-items">
              <%= link_to "Login", new_admin_session_path %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>

    <div class="container mt-4 padding">
      <%= yield %>
    </div>
  </body>
</html>
